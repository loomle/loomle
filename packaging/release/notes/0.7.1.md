# Loomle 0.7.1

Loomle 0.7.1 is a maintenance release for the stable SAL v3 line.

## Supported environment

- Unreal Engine 5.7
- macOS on Apple Silicon
- Windows on x64
- Engine-plugin installation

## Fixes

- Removed Loomle's dependency on `FJsonObject` using `FString` as its internal
  key-storage type. Field lookup now uses Unreal's supported accessor, and key
  enumeration remains compatible with the UE 5.7 representation.
- Hardened release promotion so the maintainer-created tag must resolve to the
  exact Mac- and Windows-verified commit.

## Compatibility

SAL v3 continues to use Client–Bridge protocol version 3. This release remains
supported on Unreal Engine 5.7; the JSON storage change is forward-compatibility
hardening and does not declare support for Unreal Engine 5.8.

## macOS security notice

The bundled agent-invoked Client and Bridge are not signed with a Loomle
Developer ID certificate or notarized by Apple. macOS Gatekeeper may therefore
require explicit approval before the Client can run.

## Windows security notice

The bundled agent-invoked Client and Bridge are not Authenticode-signed.
Windows may therefore show a trust or SmartScreen warning, and restrictive
Smart App Control policies may block execution until the files are approved.
