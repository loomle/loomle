# Patch variant for removing one data connection.
bp = asset(path: "/Game/BP_SALExample", type: blueprint)
g = graph(domain: blueprint, asset: bp, graph: EventGraph)
patch g

disconnect threshold.ReturnValue -> branch.Condition
