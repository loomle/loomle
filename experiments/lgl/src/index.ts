/**
 * LGL SDK experiment entrypoint.
 *
 * The package intentionally exposes no runtime API yet. The current experiment
 * is design-first: docs and examples define the SDK facade, adapter contract,
 * and LGL text forms before parser or adapter code is implemented.
 */
export {};
