{
  "name": "@loomle/lgl-experiment",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Experimental TypeScript prototype for Loomle Graph Lang.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node dist/tests/golden/run.js",
    "parse": "npm run build && node dist/src/cli.js parse",
    "compile": "npm run build && node dist/src/cli.js compile"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
