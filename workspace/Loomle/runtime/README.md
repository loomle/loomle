# LOOMLE Runtime

This directory is reserved for local runtime state, generated config, or other workspace-local operational files.

It is installable as an empty scaffold and should not be treated as a source-code directory.
