{
  "name": "loomle",
  "version": "0.7.0-dev.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "sal",
    "interfaces",
    "client"
  ],
  "scripts": {
    "generate:version": "node packaging/tools/product-version.mjs generate",
    "check:version": "node packaging/tools/product-version.mjs check",
    "build": "npm run check:version && npm run build --workspace @loomle/client",
    "test": "npm run check:version && node --test packaging/tools/product-version.test.mjs && npm run test --workspace @loomle/sal && npm run test --workspace @loomle/interfaces && npm run test --workspace @loomle/client"
  }
}
