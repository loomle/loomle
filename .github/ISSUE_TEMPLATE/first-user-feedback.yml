name: First user feedback
description: Share what happened when you tried LOOMLE with an Unreal project.
title: "[Feedback] "
labels:
  - feedback
body:
  - type: markdown
    attributes:
      value: |
        Thanks for trying LOOMLE. The most useful feedback is concrete: what you tried, where it worked, and where it became unclear or failed.
  - type: textarea
    id: goal
    attributes:
      label: What were you trying to do?
      description: Example: attach to a UE project, inspect a Blueprint, add a Material node, edit a PCG parameter.
    validations:
      required: true
  - type: textarea
    id: result
    attributes:
      label: What happened?
      description: Include the first confusing step, error message, or successful operation.
    validations:
      required: true
  - type: input
    id: host
    attributes:
      label: MCP host
      description: Codex, Claude, another MCP host, or direct client testing.
  - type: input
    id: unreal
    attributes:
      label: Unreal Engine version
      description: Example: UE 5.7.
  - type: textarea
    id: logs
    attributes:
      label: Relevant output
      description: Paste `loomle doctor`, tool output, diagnostics, or logs if useful.
      render: text
