name: Verify Loomle Fab Plugin (Mac)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: verify-loomle-fab-mac
  cancel-in-progress: false

jobs:
  verify-fab-mac:
    runs-on: [self-hosted, macOS]
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        unreal: ["5.7", "5.8"]
    env:
      UE_VERSION: ${{ matrix.unreal }}
      UE_SLUG: ue${{ matrix.unreal }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Require Apple Silicon Runner
        shell: bash
        run: |
          set -euo pipefail
          [[ "$(uname -m)" == "arm64" ]] || {
            echo "The accepted darwin-arm64 Client must be built on an Apple Silicon runner." >&2
            exit 1
          }

      - name: Set Up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install Dependencies
        run: npm ci

      - name: Verify Workspaces And Fab Assembler
        run: npm test

      - name: Build And Test Standalone Client
        env:
          LOOMLE_CLIENT_RUNTIME_CACHE: ${{ runner.tool_cache }}/loomle-client-runtime
        run: |
          npm run build:executable -- --target darwin-arm64
          npm run test:executable -- --target darwin-arm64

      - name: Assemble Fab Plugin
        id: assemble
        shell: bash
        run: |
          set -euo pipefail
          staging="$RUNNER_TEMP/loomle-fab-$UE_SLUG-darwin-arm64"
          node packaging/fab/assemble.mjs \
            --repo-root "$GITHUB_WORKSPACE" \
            --output-dir "$staging" \
            --target darwin-arm64
          echo "staging=$staging" >> "$GITHUB_OUTPUT"

      - name: Archive Fab Source Package
        id: source-archive
        shell: bash
        env:
          STAGING: ${{ steps.assemble.outputs.staging }}
        run: |
          set -euo pipefail
          archive="$RUNNER_TEMP/loomle-fab-source-$UE_SLUG-darwin-arm64.zip"
          rm -f "$archive"
          cd "$STAGING"
          /usr/bin/zip -qry "$archive" LoomleBridge

          entries="$RUNNER_TEMP/loomle-fab-source-entries-$UE_SLUG-darwin-arm64.txt"
          /usr/bin/unzip -Z1 "$archive" > "$entries"
          for required in \
            LoomleBridge/LoomleBridge.uplugin \
            LoomleBridge/Source/LoomleBridge/LoomleBridge.Build.cs \
            LoomleBridge/Resources/Loomle/darwin-arm64/loomle \
            LoomleBridge/Content/ \
            LoomleBridge/LICENSE \
            LoomleBridge/THIRD_PARTY_NOTICES.txt; do
            grep -Fxq "$required" "$entries" || {
              echo "Fab source archive is missing $required." >&2
              exit 1
            }
          done
          if grep -Eq '^LoomleBridge/(Binaries|Intermediate|Saved)(/|$)' "$entries"; then
            echo "Fab source archive contains UE-generated output." >&2
            exit 1
          fi

          audit="$RUNNER_TEMP/loomle-fab-source-audit-$UE_SLUG-darwin-arm64"
          rm -rf "$audit"
          mkdir -p "$audit"
          /usr/bin/unzip -q "$archive" -d "$audit"
          [[ -x "$audit/LoomleBridge/Resources/Loomle/darwin-arm64/loomle" ]] || {
            echo "Fab source archive did not preserve the Client executable bit." >&2
            exit 1
          }

          archive_sha="$archive.sha256"
          archive_hash="$(shasum -a 256 "$archive" | awk '{print $1}')"
          printf '%s  %s\n' "$archive_hash" "$(basename "$archive")" > "$archive_sha"
          echo "archive=$archive" >> "$GITHUB_OUTPUT"
          echo "archive_sha=$archive_sha" >> "$GITHUB_OUTPUT"

      - name: Resolve Unreal Engine Root
        id: ue
        shell: bash
        env:
          UE_5_7_ROOT: ${{ vars.UE_5_7_ROOT_MAC }}
          UE_5_8_ROOT: ${{ vars.UE_5_8_ROOT_MAC }}
        run: |
          set -euo pipefail
          case "$UE_VERSION" in
            5.7) candidate="${UE_5_7_ROOT:-/Users/Shared/Epic Games/UE_5.7}" ;;
            5.8) candidate="${UE_5_8_ROOT:-/Users/Shared/Epic Games/UE_5.8}" ;;
            *) echo "Unsupported UE version: $UE_VERSION" >&2; exit 1 ;;
          esac
          run_uat="$candidate/Engine/Build/BatchFiles/RunUAT.sh"
          [[ -x "$run_uat" ]] || {
            echo "RunUAT.sh not found: $run_uat" >&2
            exit 1
          }
          node -e '
            const build = JSON.parse(require("node:fs").readFileSync(process.argv[1], "utf8"));
            const actual = `${build.MajorVersion}.${build.MinorVersion}`;
            if (actual !== process.argv[2]) throw new Error(`Engine reports ${actual}; expected ${process.argv[2]}.`);
          ' "$candidate/Engine/Build/Build.version" "$UE_VERSION"
          echo "ue_root=$candidate" >> "$GITHUB_OUTPUT"
          echo "run_uat=$run_uat" >> "$GITHUB_OUTPUT"

      - name: Prepare Single Tested Plugin Input
        id: tested-input
        shell: bash
        env:
          STAGING: ${{ steps.assemble.outputs.staging }}
        run: |
          set -euo pipefail
          tested_input="$RUNNER_TEMP/loomle-tested-input-$UE_SLUG-darwin-arm64"
          node packaging/fab/tested-plugin.mjs prepare \
            --repo-root "$GITHUB_WORKSPACE" \
            --source-plugin "$STAGING/LoomleBridge" \
            --output-dir "$tested_input"
          echo "plugin=$tested_input/LoomleBridge" >> "$GITHUB_OUTPUT"

      - name: Compile Single Tested Plugin
        id: package
        timeout-minutes: 30
        shell: bash
        env:
          RUN_UAT: ${{ steps.ue.outputs.run_uat }}
          TESTED_PLUGIN: ${{ steps.tested-input.outputs.plugin }}
        run: |
          set -euo pipefail
          package="$RUNNER_TEMP/loomle-fab-buildplugin-$UE_SLUG-darwin-arm64"
          rm -rf "$package"
          "$RUN_UAT" BuildPlugin \
            -Plugin="$TESTED_PLUGIN/LoomleBridge.uplugin" \
            -Package="$package" \
            -PackageAppendPluginSubdir \
            -TargetPlatforms=Mac \
            -Architecture_Mac=arm64 \
            -Rocket
          test -d "$package/LoomleBridge/Source/LoomleBridgeTests"
          echo "plugin=$package/LoomleBridge" >> "$GITHUB_OUTPUT"
          echo "package=$package" >> "$GITHUB_OUTPUT"

      - name: Require Idle Unreal Editor Before Automation
        shell: bash
        run: |
          set -euo pipefail
          if pgrep -x UnrealEditor >/dev/null || pgrep -x UnrealEditor-Cmd >/dev/null; then
            echo "The self-hosted runner already has an Unreal Editor process." >&2
            pgrep -fl 'UnrealEditor|UnrealEditor-Cmd' >&2 || true
            exit 1
          fi

      - name: Run Complete UE Automation
        timeout-minutes: 15
        shell: bash
        env:
          UE_ROOT: ${{ steps.ue.outputs.ue_root }}
          AUTOMATION_PLUGIN: ${{ steps.package.outputs.plugin }}
        run: |
          set -euo pipefail
          results="$RUNNER_TEMP/loomle-automation-results-$UE_SLUG-darwin-arm64"
          rm -rf "$results"
          npm run test:ue-automation -- \
            --ue-root "$UE_ROOT" \
            --project-template "$GITHUB_WORKSPACE/tests/fixtures/ue/LoomleTestHost" \
            --plugin-dir "$AUTOMATION_PLUGIN" \
            --output-dir "$results" \
            --target darwin-arm64 \
            --phase-deadline-seconds 600
          node -e '
            const result = JSON.parse(require("node:fs").readFileSync(process.argv[1], "utf8"));
            if (result.status !== "passed" || result.commit !== process.env.GITHUB_SHA
                || result.engineVersion !== process.env.UE_VERSION) {
              throw new Error("UE Automation result does not match the checked-out commit and engine.");
            }
          ' "$results/result.json"

      - name: Finalize Exact Automation-Tested Plugin
        shell: bash
        env:
          PACKAGE: ${{ steps.package.outputs.package }}
        run: |
          set -euo pipefail
          node packaging/fab/tested-plugin.mjs finalize \
            --plugin-dir "$PACKAGE/LoomleBridge" \
            --target darwin-arm64 \
            --receipt "$RUNNER_TEMP/loomle-tested-plugin-$UE_SLUG-darwin-arm64.json"
          mkdir -p "$PACKAGE/LoomleBridge/Content"

      - name: Audit Packaged Client
        shell: bash
        env:
          PACKAGE: ${{ steps.package.outputs.package }}
          STAGING: ${{ steps.assemble.outputs.staging }}
        run: |
          set -euo pipefail
          packaged_plugin="$PACKAGE/LoomleBridge"
          source_client="$STAGING/LoomleBridge/Resources/Loomle/darwin-arm64/loomle"
          packaged_client="$packaged_plugin/Resources/Loomle/darwin-arm64/loomle"
          [[ -x "$packaged_client" ]] || {
            echo "BuildPlugin output is missing the executable Client: $packaged_client" >&2
            exit 1
          }
          [[ ! -e "$packaged_plugin/Resources/MCP" ]] || {
            echo "BuildPlugin output contains retired Resources/MCP." >&2
            exit 1
          }
          [[ ! -e "$packaged_plugin/Resources/Loomle/darwin-arm64/build.json" ]] || {
            echo "BuildPlugin output must not ship the internal Client build receipt." >&2
            exit 1
          }
          for forbidden_path in \
            Source/LoomleBridgeTests \
            Intermediate \
            Saved; do
            [[ ! -e "$packaged_plugin/$forbidden_path" ]] || {
              echo "BuildPlugin output contains forbidden release path: $forbidden_path" >&2
              exit 1
            }
          done
          [[ -d "$packaged_plugin/Content" ]] || {
            echo "BuildPlugin output is missing the required empty Content directory." >&2
            exit 1
          }
          [[ -z "$(find "$packaged_plugin/Content" -mindepth 1 -print -quit)" ]] || {
            echo "BuildPlugin output Content directory must be empty." >&2
            exit 1
          }
          for required_file in LICENSE THIRD_PARTY_NOTICES.txt; do
            [[ -s "$packaged_plugin/$required_file" ]] || {
              echo "BuildPlugin output is missing $required_file." >&2
              exit 1
            }
            [[ "$(shasum -a 256 "$STAGING/LoomleBridge/$required_file" | awk '{print $1}')" == \
               "$(shasum -a 256 "$packaged_plugin/$required_file" | awk '{print $1}')" ]] || {
              echo "BuildPlugin changed $required_file." >&2
              exit 1
            }
          done
          [[ -f "$packaged_plugin/Config/FilterPlugin.ini" ]] || {
            echo "BuildPlugin output is missing Config/FilterPlugin.ini." >&2
            exit 1
          }
          client_target_count="$(find "$packaged_plugin/Resources/Loomle" -mindepth 1 -maxdepth 1 -type d | wc -l | tr -d ' ')"
          [[ "$client_target_count" == "1" ]] || {
            echo "BuildPlugin output must contain exactly one Client target." >&2
            exit 1
          }
          [[ "$(shasum -a 256 "$source_client" | awk '{print $1}')" == \
             "$(shasum -a 256 "$packaged_client" | awk '{print $1}')" ]] || {
            echo "BuildPlugin changed the bundled Client." >&2
            exit 1
          }
          "$packaged_client" --help

          descriptor="$packaged_plugin/LoomleBridge.uplugin"
          node -e '
            const descriptor = JSON.parse(require("node:fs").readFileSync(process.argv[1], "utf8"));
            const moduleNames = descriptor.Modules?.map((entry) => entry.Name);
            const module = descriptor.Modules?.[0];
            const same = (actual, expected) => JSON.stringify(actual) === JSON.stringify(expected);
            if (descriptor.Installed !== true
                || !same(descriptor.SupportedTargetPlatforms, ["Mac"])
                || !same(moduleNames, ["LoomleBridge"])
                || !same(module?.PlatformAllowList, ["Mac"])
                || module?.PlatformArchitectureAllowList !== undefined) {
              throw new Error("BuildPlugin output has an invalid target descriptor contract.");
            }
          ' "$descriptor"

          bridge_name="$(node -e '
            const manifest = JSON.parse(require("node:fs").readFileSync(process.argv[1], "utf8"));
            const name = manifest.Modules?.LoomleBridge;
            if (typeof name !== "string" || !/^(?:lib)?UnrealEditor-LoomleBridge\.dylib$/.test(name)) {
              throw new Error("Invalid LoomleBridge module manifest entry.");
            }
            process.stdout.write(name);
          ' "$packaged_plugin/Binaries/Mac/UnrealEditor.modules")"
          bridge_binary="$packaged_plugin/Binaries/Mac/$bridge_name"
          [[ -f "$bridge_binary" ]] || {
            echo "BuildPlugin output is missing the Mac Bridge binary: $bridge_binary" >&2
            exit 1
          }
          [[ "$(/usr/bin/lipo -archs "$bridge_binary")" == "arm64" ]] || {
            echo "BuildPlugin output is not arm64-only: $bridge_binary" >&2
            /usr/bin/lipo -archs "$bridge_binary" >&2
            exit 1
          }

      - name: Verify GitHub Package Derives From Fab Source
        shell: bash
        env:
          PACKAGE: ${{ steps.package.outputs.package }}
          STAGING: ${{ steps.assemble.outputs.staging }}
        run: |
          set -euo pipefail
          node packaging/fab/verify-derivation.mjs \
            --source-plugin "$STAGING/LoomleBridge" \
            --github-plugin "$PACKAGE/LoomleBridge" \
            --engine-version "$UE_VERSION" \
            --target darwin-arm64

      - name: Archive Verified BuildPlugin Output
        id: archive
        shell: bash
        env:
          PACKAGE: ${{ steps.package.outputs.package }}
        run: |
          set -euo pipefail
          archive="$RUNNER_TEMP/loomle-fab-plugin-$UE_SLUG-darwin-arm64.zip"
          rm -f "$archive"
          cd "$PACKAGE"
          /usr/bin/zip -qry "$archive" LoomleBridge
          archive_entries="$RUNNER_TEMP/loomle-fab-archive-entries-$UE_SLUG-darwin-arm64.txt"
          /usr/bin/unzip -Z1 "$archive" > "$archive_entries"
          grep -Fxq 'LoomleBridge/Resources/Loomle/darwin-arm64/loomle' "$archive_entries"
          grep -Fxq 'LoomleBridge/Content/' "$archive_entries"
          grep -Fxq 'LoomleBridge/LICENSE' "$archive_entries"
          grep -Fxq 'LoomleBridge/THIRD_PARTY_NOTICES.txt' "$archive_entries"
          if grep -Eq '^LoomleBridge/Resources/MCP(/|$)' "$archive_entries"; then
            echo "Fab archive contains retired Resources/MCP." >&2
            exit 1
          fi
          if grep -Fxq 'LoomleBridge/Resources/Loomle/darwin-arm64/build.json' "$archive_entries"; then
            echo "Fab archive contains the internal Client build receipt." >&2
            exit 1
          fi
          for forbidden_path in \
            Source/LoomleBridgeTests \
            Intermediate \
            Saved; do
            if grep -Eq "^LoomleBridge/$forbidden_path(/|$)" "$archive_entries"; then
              echo "Fab archive contains forbidden release path: $forbidden_path" >&2
              exit 1
            fi
          done
          if grep -E '^LoomleBridge/Content/.+' "$archive_entries"; then
            echo "Fab archive Content directory must be empty." >&2
            exit 1
          fi

          audit="$RUNNER_TEMP/loomle-fab-archive-audit-$UE_SLUG-darwin-arm64"
          rm -rf "$audit"
          mkdir -p "$audit"
          /usr/bin/unzip -q "$archive" -d "$audit"
          packaged_plugin="$PACKAGE/LoomleBridge"
          extracted_plugin="$audit/LoomleBridge"
          bridge_name="$(node -e '
            const manifest = JSON.parse(require("node:fs").readFileSync(process.argv[1], "utf8"));
            const name = manifest.Modules?.LoomleBridge;
            if (typeof name !== "string" || !/^(?:lib)?UnrealEditor-LoomleBridge\.dylib$/.test(name)) process.exit(1);
            process.stdout.write(name);
          ' "$packaged_plugin/Binaries/Mac/UnrealEditor.modules")"
          for relative_path in \
            LoomleBridge.uplugin \
            Config/FilterPlugin.ini \
            LICENSE \
            THIRD_PARTY_NOTICES.txt \
            Resources/Loomle/darwin-arm64/loomle \
            Binaries/Mac/UnrealEditor.modules \
            "Binaries/Mac/$bridge_name"; do
            [[ "$(shasum -a 256 "$packaged_plugin/$relative_path" | awk '{print $1}')" == \
               "$(shasum -a 256 "$extracted_plugin/$relative_path" | awk '{print $1}')" ]] || {
              echo "Fab archive changed or omitted $relative_path." >&2
              exit 1
            }
          done
          [[ -x "$extracted_plugin/Resources/Loomle/darwin-arm64/loomle" ]] || {
            echo "Fab archive did not preserve the Client executable bit." >&2
            exit 1
          }
          [[ "$(/usr/bin/lipo -archs "$extracted_plugin/Binaries/Mac/$bridge_name")" == "arm64" ]] || {
            echo "Fab archive Bridge is not arm64-only." >&2
            exit 1
          }
          node -e '
            const descriptor = JSON.parse(require("node:fs").readFileSync(process.argv[1], "utf8"));
            const moduleNames = descriptor.Modules?.map((entry) => entry.Name);
            if (JSON.stringify(moduleNames) !== JSON.stringify(["LoomleBridge"])
                || descriptor.Modules?.[0]?.PlatformArchitectureAllowList !== undefined) {
              throw new Error("Fab archive descriptor has an invalid module contract.");
            }
          ' "$extracted_plugin/LoomleBridge.uplugin"
          archive_sha="$archive.sha256"
          archive_hash="$(shasum -a 256 "$archive" | awk '{print $1}')"
          printf '%s  %s\n' "$archive_hash" "$(basename "$archive")" > "$archive_sha"
          echo "archive=$archive" >> "$GITHUB_OUTPUT"
          echo "archive_sha=$archive_sha" >> "$GITHUB_OUTPUT"

      - name: Require Idle Unreal Editor Before Packaged End-To-End
        shell: bash
        run: |
          set -euo pipefail
          if pgrep -x UnrealEditor >/dev/null || pgrep -x UnrealEditor-Cmd >/dev/null; then
            echo "The self-hosted runner already has an Unreal Editor process." >&2
            pgrep -fl 'UnrealEditor|UnrealEditor-Cmd' >&2 || true
            exit 1
          fi

      - name: Run Packaged End-To-End
        timeout-minutes: 15
        shell: bash
        env:
          UE_ROOT: ${{ steps.ue.outputs.ue_root }}
          ARCHIVE: ${{ steps.archive.outputs.archive }}
        run: |
          set -euo pipefail
          results="$RUNNER_TEMP/loomle-packaged-e2e-results-$UE_SLUG-darwin-arm64"
          rm -rf "$results"
          npm run test:packaged-e2e -- \
            --ue-root "$UE_ROOT" \
            --project-template "$GITHUB_WORKSPACE/tests/fixtures/ue/LoomleTestHost" \
            --plugin-archive "$ARCHIVE" \
            --output-dir "$results" \
            --target darwin-arm64 \
            --phase-deadline-seconds 600
          archive_sha="$(shasum -a 256 "$ARCHIVE" | awk '{print $1}')"
          node -e '
            const result = JSON.parse(require("node:fs").readFileSync(process.argv[1], "utf8"));
            if (result.status !== "passed"
                || result.commit !== process.env.GITHUB_SHA
                || result.engineVersion !== process.env.UE_VERSION
                || result.archiveSha256 !== process.argv[2]) {
              throw new Error("Packaged E2E result does not match the checked-out commit, engine, and exact release archive.");
            }
          ' "$results/result.json" "$archive_sha"

      - name: Upload Native Test Results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: loomle-native-test-results-${{ env.UE_SLUG }}-darwin-arm64
          path: |
            ${{ runner.temp }}/loomle-automation-results-${{ env.UE_SLUG }}-darwin-arm64
            ${{ runner.temp }}/loomle-packaged-e2e-results-${{ env.UE_SLUG }}-darwin-arm64
          if-no-files-found: warn

      - name: Upload QA Artifact
        uses: actions/upload-artifact@v7
        with:
          name: loomle-fab-plugin-${{ env.UE_SLUG }}-darwin-arm64
          path: |
            ${{ steps.archive.outputs.archive }}
            ${{ steps.archive.outputs.archive_sha }}
          if-no-files-found: error

      - name: Upload Fab Source Artifact
        if: matrix.unreal == '5.7'
        uses: actions/upload-artifact@v7
        with:
          name: loomle-fab-source-darwin-arm64
          path: |
            ${{ steps.source-archive.outputs.archive }}
            ${{ steps.source-archive.outputs.archive_sha }}
          if-no-files-found: error
